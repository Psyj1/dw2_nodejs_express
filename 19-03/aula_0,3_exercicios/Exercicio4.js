/*
4 – Crie uma função que receba um nome como parâmetro e exiba no console este nome formatado somente com letras maiúsculas, somente com letras minúsculas, e em seguida exiba o número de letras que esse nome possui.
*/

const name = "Paulo";

console.log(name.toUpperCase());

console.log(name.toLocaleLowerCase())

console.log(name.length)